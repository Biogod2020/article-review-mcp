export const UI_URI = "ui://article-review/review.html";
export const UI_MIME = "text/html;profile=mcp-app";

export function uiHtml({ bootstrap = null, standalone = false } = {}) {
  const initial = JSON.stringify(bootstrap).replaceAll("<", "\\u003c");
  return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Article Review MCP</title><style>
:root{color-scheme:light dark;--bg:#eef1f5;--paper:#fff;--ink:#202733;--muted:#687386;--line:#d9dee7;--blue:#2563eb;--green:#067647;--greenbg:#dcfae6;--red:#b42318;--redbg:#fee4e2;--amber:#b54708;--amberbg:#ffead5}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:13px/1.55 system-ui,-apple-system,"Segoe UI","PingFang SC",sans-serif}body.dark{--bg:#111827;--paper:#182232;--ink:#e5e7eb;--muted:#9ca3af;--line:#344155;--greenbg:#123a2a;--redbg:#482423;--amberbg:#4b3519}.bar{position:sticky;top:0;z-index:10;display:flex;align-items:center;gap:7px;flex-wrap:wrap;padding:10px 14px;background:var(--paper);border-bottom:1px solid var(--line)}.title{min-width:220px;flex:1}.title b{display:block;font-size:15px}.summary{font-size:11px;color:var(--muted)}button,input,select,textarea{font:inherit;color:inherit}.control{padding:6px 9px;border:1px solid var(--line);border-radius:7px;background:var(--paper)}button{cursor:pointer}.active{outline:2px solid color-mix(in srgb,var(--blue) 30%,transparent)}.workspace{display:grid;grid-template-columns:minmax(0,1fr) 340px;gap:14px;max-width:1750px;margin:auto;padding:14px}.paper,.panel{background:var(--paper);border:1px solid var(--line);border-radius:9px;overflow:hidden}.legend,.split{display:grid;grid-template-columns:1fr 1fr}.legend{color:var(--muted);font-size:11px;border-bottom:1px solid var(--line)}.legend div,.cell{padding:9px 12px}.legend div+div,.after{border-left:1px solid var(--line)}.hunk{border-bottom:1px solid var(--line);scroll-margin-top:75px}.hunk.selected{outline:2px solid color-mix(in srgb,var(--blue) 55%,transparent);outline-offset:-2px}.head{display:flex;align-items:center;gap:7px;padding:6px 9px;background:color-mix(in srgb,var(--paper) 92%,var(--bg));border-bottom:1px solid var(--line)}.path{min-width:0;flex:1;color:var(--muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tag{font-size:10px;font-weight:700;padding:2px 7px;border-radius:999px}.pending{background:var(--amberbg);color:var(--amber)}.accepted{background:var(--greenbg);color:var(--green)}.rejected{background:var(--redbg);color:var(--red)}.edited{background:#e0e7ff;color:#4338ca}.mini{padding:3px 7px;border:1px solid var(--line);border-radius:6px;background:var(--paper)}.cell{min-width:0;min-height:50px;white-space:pre-wrap;overflow-wrap:anywhere;font:13px/1.65 Georgia,"Times New Roman","Songti SC",serif}.ins{background:var(--greenbg);color:var(--green)}.del{background:var(--redbg);color:var(--red);text-decoration:line-through}.unified .split{display:block}.unified .after{border-left:0;border-top:1px solid var(--line)}.final .before{display:none}.final .split{display:block}.final .after{border:0}.aside{position:sticky;top:74px;max-height:calc(100vh - 90px);overflow:auto}.panel h3{font-size:13px;margin:0;padding:9px 11px;border-bottom:1px solid var(--line)}.comments{padding:8px}.comment{padding:8px;margin-bottom:8px;border:1px solid var(--line);border-left:4px solid var(--blue);border-radius:7px}.meta{display:flex;justify-content:space-between;font-size:10px;color:var(--muted)}.body{white-space:pre-wrap}.reply{margin-top:6px;padding:6px;background:color-mix(in srgb,var(--blue) 9%,var(--paper));border-radius:5px;font-size:11px}.form{padding:9px;border-top:1px solid var(--line)}.form input,.form textarea{width:100%;margin-bottom:6px;padding:7px;border:1px solid var(--line);border-radius:6px;background:var(--paper)}.form textarea{min-height:75px}.empty{padding:50px 15px;text-align:center;color:var(--muted)}.editor{padding:9px;border-top:1px solid var(--line)}.editor textarea{width:100%;min-height:120px;padding:8px;border:1px solid var(--line);border-radius:6px;background:var(--paper);font-family:monospace}.error{margin:10px 14px;padding:8px;border:1px solid var(--red);border-radius:7px;background:var(--redbg);color:var(--red)}.hidden{display:none}.busy{opacity:.55;pointer-events:none}@media(max-width:760px){.workspace{display:block;padding:8px}.aside{position:static;max-height:none;margin-top:8px}.split,.legend{grid-template-columns:1fr}.after,.legend div+div{border-left:0;border-top:1px solid var(--line)}.bar{position:static}}
</style></head><body><header class="bar"><div class="title"><b id="title">Article Review MCP</b><span class="summary" id="summary">Waiting for data…</span></div><button class="control active mode" data-mode="split">Split</button><button class="control mode" data-mode="unified">Unified</button><button class="control mode" data-mode="final">Final</button><select id="filter" class="control"><option value="all">All</option><option value="pending">Pending</option><option value="accepted">Accepted</option><option value="rejected">Rejected</option><option value="edited">Edited</option></select><input id="search" class="control" placeholder="Search changes…"><button id="acceptAll" class="control">Accept all</button><button id="rejectAll" class="control">Reject all</button></header><div id="error" class="error hidden"></div><main class="workspace"><section id="paper" class="paper"><div class="legend"><div>Before</div><div>Proposed / selected</div></div><div id="hunks"><div class="empty">No review loaded.</div></div></section><aside class="aside"><section class="panel"><h3>Reviewer comments</h3><div id="comments" class="comments"><div class="empty">Select a change.</div></div><form id="form" class="form"><input id="reviewer" value="Human reviewer" placeholder="Reviewer"><textarea id="comment" placeholder="Comment on selected change"></textarea><button class="control" type="submit">Add comment</button></form></section></aside></main><script>
const INITIAL=${initial},STANDALONE=${standalone};const state={data:INITIAL,selected:null,mode:'split',status:'all',query:'',busy:false};const $=id=>document.getElementById(id);let rpcId=1;const pending=new Map();
function key(){return crypto.randomUUID?.()||String(Date.now())+Math.random()}function err(e){$('error').textContent=e?.message||String(e);$('error').classList.remove('hidden')}function clear(){ $('error').classList.add('hidden') }function structured(x){return x?.structuredContent||x?.result?.structuredContent||x}
window.addEventListener('message',event=>{const m=event.data;if(!m||m.jsonrpc!=='2.0')return;if(Object.hasOwn(m,'id')&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}else if(m.method==='ui/notifications/tool-result')apply(structured(m.params));else if(m.method==='ui/notifications/host-context-changed')theme(m.params)});
function request(method,params={}){const id=rpcId++;parent.postMessage({jsonrpc:'2.0',id,method,params},'*');return new Promise((resolve,reject)=>{pending.set(id,{resolve,reject});setTimeout(()=>{if(pending.delete(id))reject(new Error('Host timeout'))},30000)})}function notify(method,params={}){parent.postMessage({jsonrpc:'2.0',method,params},'*')}
async function tool(name,args){if(STANDALONE){const token=new URLSearchParams(location.search).get('token');const response=await fetch('/api/tool'+(token?'?token='+encodeURIComponent(token):''),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,arguments:args})});const value=await response.json();if(!response.ok||value.isError)throw new Error(value.content?.[0]?.text||value.error||'Request failed');return value}const value=await request('tools/call',{name,arguments:args});if(value?.isError)throw new Error(value.content?.[0]?.text||'Tool failed');return value}
async function call(name,args){clear();document.body.classList.add('busy');try{const result=await tool(name,args);apply(structured(result))}catch(e){err(e)}finally{document.body.classList.remove('busy')}}function theme(c){document.body.classList.toggle('dark',c?.theme==='dark')}
function apply(data){if(!data?.sessionId)return;state.data=data;if(!data.page.items.some(h=>h.id===state.selected))state.selected=data.page.items[0]?.id||null;render()}
function text(node,value){node.append(document.createTextNode(value||''))}function button(label,handler){const b=document.createElement('button');b.className='mini';b.type='button';b.textContent=label;b.onclick=e=>{e.stopPropagation();handler()};return b}
function decide(id,status){call('article_review_set_decision',{sessionId:state.data.sessionId,hunkId:id,status,expectedVersion:state.data.version,idempotencyKey:key()})}function bulk(status){call('article_review_bulk_decide',{sessionId:state.data.sessionId,status,expectedVersion:state.data.version,idempotencyKey:key()})}
function edit(h){const row=$('hunk-'+h.id),old=$('edit-'+h.id);if(old){old.remove();return}const wrap=document.createElement('div');wrap.className='editor';wrap.id='edit-'+h.id;const area=document.createElement('textarea');area.value=h.decision.status==='edited'?(h.decision.editedText||''):h.afterText;wrap.append(area,button('Save',()=>call('article_review_edit_hunk',{sessionId:state.data.sessionId,hunkId:h.id,editedText:area.value,expectedVersion:state.data.version,idempotencyKey:key()})),button('Cancel',()=>wrap.remove()));row.append(wrap);area.focus()}
function render(){const d=state.data;if(!d)return;$('title').textContent=d.title;const c=d.counts;$('summary').textContent=c.total+' changes · '+c.accepted+' accepted · '+c.rejected+' rejected · '+c.edited+' edited · '+c.pending+' pending · v'+d.version;$('paper').className='paper '+state.mode;document.querySelectorAll('.mode').forEach(b=>b.classList.toggle('active',b.dataset.mode===state.mode));const root=$('hunks');root.replaceChildren();if(!d.page.items.length){const x=document.createElement('div');x.className='empty';x.textContent='No matching changes.';root.append(x)}for(const h of d.page.items){const row=document.createElement('article');row.id='hunk-'+h.id;row.className='hunk'+(h.id===state.selected?' selected':'');row.onclick=()=>{state.selected=h.id;render()};const head=document.createElement('div');head.className='head';const p=document.createElement('div');p.className='path';p.textContent=h.sectionPath.join(' › ')||'Document';const tag=document.createElement('span');tag.className='tag '+h.decision.status;tag.textContent=h.decision.status;head.append(p,tag,button('Accept',()=>decide(h.id,'accepted')),button('Reject',()=>decide(h.id,'rejected')),button('Edit',()=>edit(h)),button('Reset',()=>decide(h.id,'pending')));const split=document.createElement('div');split.className='split';const before=document.createElement('div');before.className='cell before';text(before,h.beforeText);const after=document.createElement('div');after.className='cell after';if(h.decision.status==='rejected')text(after,h.beforeText);else if(h.decision.status==='edited')text(after,h.decision.editedText||'');else for(const op of h.inlineOps){if(op.type==='delete')continue;const s=document.createElement('span');if(op.type==='insert')s.className='ins';s.textContent=op.text;after.append(s)}split.append(before,after);row.append(head,split);root.append(row)}renderComments()}
function renderComments(){const root=$('comments');root.replaceChildren();const h=state.data?.page.items.find(x=>x.id===state.selected);if(!h||!h.comments.length){const x=document.createElement('div');x.className='empty';x.textContent=h?'No comments.':'Select a change.';root.append(x);return}for(const c of h.comments){const card=document.createElement('article');card.className='comment';const meta=document.createElement('div');meta.className='meta';const a=document.createElement('b');a.textContent=c.author?.name||'Reviewer';const k=document.createElement('span');k.textContent=c.category;meta.append(a,k);const body=document.createElement('div');body.className='body';body.textContent=c.body;card.append(meta,body);if(c.implementationReply){const r=document.createElement('div');r.className='reply';r.textContent='Modification implemented: '+c.implementationReply;card.append(r)}root.append(card)}}
document.querySelectorAll('.mode').forEach(b=>b.onclick=()=>{state.mode=b.dataset.mode;render()});$('filter').onchange=e=>{state.status=e.target.value;call('article_review_get_page',{sessionId:state.data.sessionId,status:state.status,query:state.query})};let timer;$('search').oninput=e=>{state.query=e.target.value;clearTimeout(timer);timer=setTimeout(()=>call('article_review_get_page',{sessionId:state.data.sessionId,status:state.status,query:state.query}),250)};$('acceptAll').onclick=()=>bulk('accepted');$('rejectAll').onclick=()=>bulk('rejected');$('form').onsubmit=e=>{e.preventDefault();const h=state.data?.page.items.find(x=>x.id===state.selected),body=$('comment').value.trim();if(!h||!body)return;call('article_review_add_comment',{sessionId:state.data.sessionId,hunkId:h.id,body,author:{id:$('reviewer').value.toLowerCase().replace(/\W+/g,'-'),name:$('reviewer').value,role:'human'},expectedVersion:state.data.version,idempotencyKey:key()});$('comment').value=''};
document.onkeydown=e=>{if(e.target.matches('input,textarea,select'))return;const items=state.data?.page.items||[],i=Math.max(0,items.findIndex(h=>h.id===state.selected));if(e.key.toLowerCase()==='j'&&items.length){state.selected=items[Math.min(items.length-1,i+1)].id;render();$('hunk-'+state.selected)?.scrollIntoView({behavior:'smooth',block:'center'})}else if(e.key.toLowerCase()==='k'&&items.length){state.selected=items[Math.max(0,i-1)].id;render();$('hunk-'+state.selected)?.scrollIntoView({behavior:'smooth',block:'center'})}else if(e.key.toLowerCase()==='a'&&state.selected)decide(state.selected,'accepted');else if(e.key.toLowerCase()==='r'&&state.selected)decide(state.selected,'rejected');else if(e.key.toLowerCase()==='e'&&state.selected)edit(items.find(h=>h.id===state.selected))};
if(INITIAL)apply(INITIAL);if(!STANDALONE)request('ui/initialize',{appInfo:{name:'Article Review App',version:'1.0.0'},appCapabilities:{availableDisplayModes:['inline','fullscreen']},protocolVersion:'2026-01-26'}).then(r=>{theme(r.hostContext);notify('ui/notifications/initialized',{})}).catch(err);
</script></body></html>`;
}
